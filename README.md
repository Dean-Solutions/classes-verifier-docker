# classes-verifier-docker

```console
git clone --recurse-submodules https://github.com/Dean-Solutions/classes-verifier-docker.git
cd classes-verifier-docker 
cd classes-verifier-be 
./gradlew build 
cd ..
docker-compose -f docker-compose.yml
```

